# Исправление ошибки 502 Bad Gateway

## Дата: 8 февраля 2026 года

## 🎯 Проблема

При попытке открыть vezuroll.ru пользователи получали ошибку:
```
502 Bad Gateway
nginx/1.24.0 (Ubuntu)
```

## 🔍 Анализ

После анализа конфигурации был выявлен следующий корневой причиной:

1. **Nginx корректно настроен** для проксирования запросов к Laravel frontend и Python backend
2. **PHP-FPM не запускается** автоматически при развертывании
3. **Nginx не может подключиться к PHP-FPM**, что приводит к ошибке 502

## ✅ Решение

### 1. Улучшена конфигурация Nginx

**Файлы**: `nginx/iiko-base.conf`, `nginx/iiko-base-ssl.conf`

Добавлены улучшения в блок `location ~ \.php$`:
- `try_files $uri =404` - проверка существования файла перед обработкой
- `fastcgi_split_path_info` - корректная обработка путей
- Увеличены таймауты до 60 секунд для предотвращения timeout ошибок

### 2. Автоматический запуск PHP-FPM

**Файл**: `scripts/deploy.sh`

Добавлено в процесс развертывания:
```bash
# Включение PHP-FPM в автозагрузку
systemctl enable php8.1-fpm || systemctl enable php-fpm

# Перезапуск PHP-FPM
systemctl restart php8.1-fpm || systemctl restart php-fpm

# Проверка статуса
systemctl status php8.1-fpm || systemctl status php-fpm
```

### 3. Документация по устранению неисправностей

**Файл**: `docs/502_ERROR_FIX.md`

Создано подробное руководство, включающее:
- Описание причин ошибки 502
- Пошаговые инструкции по диагностике
- Быстрые решения для типичных сценариев
- Проверка логов и статуса сервисов
- Дополнительные исправления для особых случаев

### 4. Обновлен README

**Файл**: `README.md`

Добавлен раздел "Устранение неисправностей" с быстрыми ссылками на:
- Решение ошибки 502
- Решение проблем с базой данных

## 📊 Изменения в коде

```
Файлов изменено: 5
Добавлено строк: 318
Удалено строк: 2
```

**Детально**:
- `README.md` - добавлен раздел troubleshooting
- `docs/502_ERROR_FIX.md` - новый файл с полным руководством (252 строки)
- `nginx/iiko-base.conf` - улучшена конфигурация PHP-FPM
- `nginx/iiko-base-ssl.conf` - улучшена конфигурация PHP-FPM
- `scripts/deploy.sh` - добавлено управление PHP-FPM

## 🚀 Как использовать исправления

### На уже работающем сервере

Если у вас уже есть установленный сервер с ошибкой 502:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/iiko-base

# 2. Обновите код
git pull origin main

# 3. Запустите деплой (это автоматически запустит PHP-FPM)
sudo ./scripts/deploy.sh
```

### Быстрое решение без обновления кода

Если нужно быстро исправить проблему без обновления:

```bash
# Запустите PHP-FPM вручную
sudo systemctl start php8.1-fpm
sudo systemctl enable php8.1-fpm

# Перезапустите Nginx
sudo systemctl restart nginx

# Проверьте статус
sudo systemctl status php8.1-fpm
sudo systemctl status nginx
```

### При новой установке

При новой установке исправления применяются автоматически:

```bash
# Установка
sudo ./scripts/install.sh

# Настройка
./scripts/setup.sh

# Развертывание (теперь с автоматическим запуском PHP-FPM)
sudo ./scripts/deploy.sh
```

## 🔧 Диагностика

### Проверка статуса всех сервисов

```bash
# Проверка PHP-FPM
sudo systemctl status php8.1-fpm

# Проверка Python backend
sudo systemctl status iiko-backend

# Проверка Nginx
sudo systemctl status nginx

# Проверка PostgreSQL
sudo systemctl status postgresql
```

### Просмотр логов

```bash
# Логи Nginx
sudo tail -50 /var/log/nginx/error.log

# Логи PHP-FPM
sudo journalctl -u php8.1-fpm -n 50

# Логи Python backend
sudo journalctl -u iiko-backend -n 50
```

## 📝 Проверочный список для развертывания

- [x] Обновлена конфигурация Nginx с улучшенной обработкой PHP
- [x] Добавлен автоматический запуск PHP-FPM в скрипт развертывания
- [x] Создана документация по устранению неисправностей
- [x] Обновлен README с инструкциями
- [x] Прошла проверка кода (code review)
- [x] Прошла проверка безопасности (CodeQL)

## 🎓 Извлеченные уроки

1. **Всегда проверяйте статус всех зависимых сервисов** при развертывании
2. **PHP-FPM должен быть явно запущен** - не полагайтесь на автозапуск
3. **Документация по устранению неисправностей** экономит время пользователей
4. **Увеличенные таймауты** предотвращают ошибки при медленных запросах

## 📞 Дополнительная информация

Если после применения исправлений проблема не решена:

1. Прочитайте [docs/502_ERROR_FIX.md](./502_ERROR_FIX.md)
2. Проверьте логи всех сервисов
3. Убедитесь, что все зависимости установлены
4. Создайте issue в репозитории с подробным описанием

---

**Автор исправления**: GitHub Copilot  
**Дата**: 8 февраля 2026  
**Статус**: ✅ Готово к использованию  
**Тестирование**: Код проверен, синтаксис корректен
