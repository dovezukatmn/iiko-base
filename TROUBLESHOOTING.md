# 🔧 Руководство по устранению неполадок

## Проблема 1: Ошибка "Method Not Allowed" при создании пользователя

### Симптомы
При попытке создать нового пользователя в админ-панели появляется ошибка:
```
⚠️ Method Not Allowed
```

### Причины
1. Backend API недоступен
2. Неправильная настройка `BACKEND_API_URL` в Laravel
3. Backend сервис не запущен
4. Проблема с сетевым подключением между frontend и backend

### Решение

#### Шаг 1: Проверьте, что Backend запущен

**Для Docker:**
```bash
docker ps | grep iiko-backend
# Должна быть строка с контейнером iiko-backend

# Если контейнер не запущен:
docker-compose up -d backend

# Проверьте логи:
docker-compose logs backend
```

**Для systemd:**
```bash
sudo systemctl status iiko-backend
# Статус должен быть "active (running)"

# Если не запущен:
sudo systemctl start iiko-backend

# Проверьте логи:
sudo journalctl -u iiko-backend -f
```

#### Шаг 2: Проверьте настройку BACKEND_API_URL

Откройте файл `/var/www/iiko-base/frontend/.env` и проверьте:

```env
# Для локального развертывания:
BACKEND_API_URL=http://localhost:8000/api/v1

# Для Docker:
BACKEND_API_URL=http://backend:8000/api/v1

# Для production с отдельными доменами:
BACKEND_API_URL=https://api.vezuroll.ru/api/v1
```

**Важно:** URL должен быть доступен из контейнера/сервера Laravel!

#### Шаг 3: Проверьте доступность Backend API

```bash
# Из контейнера/сервера Laravel:
curl http://localhost:8000/api/v1/health
# Должно вернуть: {"status":"healthy"}

# Проверьте документацию API:
curl http://localhost:8000/api/v1/docs
# Должна открыться Swagger UI документация
```

#### Шаг 4: Проверьте настройки CORS в Backend

Откройте `/var/www/iiko-base/backend/.env` и убедитесь что домен frontend включен:

```env
BACKEND_CORS_ORIGINS='["http://localhost:3000", "https://vezuroll.ru", "https://b1d8d8270d0f.vps.myjino.ru"]'
```

#### Шаг 5: Перезапустите оба сервиса

```bash
# Docker:
docker-compose restart backend
docker-compose restart nginx

# systemd:
sudo systemctl restart iiko-backend
sudo systemctl restart php8.3-fpm
sudo systemctl restart nginx
```

---

## Проблема 2: Ошибка "Неверные учетные данные" при подключении к iiko API

### Симптомы
При тестировании подключения к iiko API появляется ошибка:
```
⚠️ Ошибка: {"detail":"Неверные учетные данные"}
```

### Причины
1. Неправильный API ключ (apiLogin)
2. API ключ истёк или был отозван
3. Использование старого формата ключа
4. Ключ скопирован не полностью или с пробелами

### Решение

#### Шаг 1: Получите новый API ключ

1. Откройте [iiko Cloud](https://api-ru.iiko.services)
2. Войдите в систему
3. Перейдите в раздел **"API"** (не "Настройки"!)
4. Нажмите **"Создать API-ключ"**
5. Скопируйте ключ **полностью** (обычно 32 символа)

#### Шаг 2: Проверьте формат ключа

Правильный API ключ выглядит примерно так:
```
28212c2c1930451d8d97892994d27fe4
```

**Типичные ошибки:**
- ❌ Скопированы лишние пробелы в начале/конце
- ❌ Ключ скопирован не полностью
- ❌ Использован старый ключ из раздела "Настройки" вместо "API"
- ❌ Ключ содержит переносы строк

#### Шаг 3: Введите ключ в админ-панели

1. Откройте админ-панель → Maintenance
2. В разделе **"iiko API Settings"**
3. Вставьте ключ в поле **"API ключ (apiLogin)"**
4. Убедитесь что URL правильный: `https://api-ru.iiko.services/api/1`
5. Нажмите **"💾 Сохранить"**
6. Нажмите **"🔌 Проверить"**

#### Шаг 4: Проверьте настройки в iiko Cloud

1. Убедитесь что API ключ **активен**
2. Проверьте что у вас есть **права на использование API**
3. Убедитесь что организация настроена правильно

#### Шаг 5: Используйте диагностику

Если ошибка повторяется, проверьте логи:

```bash
# Docker:
docker-compose logs backend | grep -i "iiko\|401\|token"

# systemd:
sudo journalctl -u iiko-backend -n 100 | grep -i "iiko\|401\|token"
```

Посмотрите таблицу логов API в админ-панели:
1. Откройте Maintenance → API Logs
2. Найдите запрос к `/access_token`
3. Проверьте статус и ответ

---

## Проблема 3: API ключ виден в интерфейсе

### Решение
✅ **Исправлено!** Теперь:
- Поле ввода API ключа имеет тип `password` (символы скрыты)
- Есть кнопка показать/скрыть (👁/🙈)
- API ключ НЕ возвращается при GET запросах к `/iiko/settings`
- При редактировании можно оставить поле пустым - ключ сохранится
- После сохранения поле автоматически очищается

---

## Дополнительные проверки

### Проверка подключения к PostgreSQL

```bash
# Проверка что база данных доступна:
psql -h localhost -U iiko_user -d iiko_db -c "SELECT COUNT(*) FROM users;"

# Должно показать количество пользователей
```

### Проверка таблицы iiko_settings

```sql
-- Подключитесь к базе:
psql -h localhost -U iiko_user -d iiko_db

-- Проверьте настройки iiko:
SELECT id, api_url, organization_id, is_active, last_token_refresh 
FROM iiko_settings;

-- НЕ выводите api_key для безопасности!
```

### Проверка структуры таблиц

```sql
-- Проверьте что все нужные колонки есть:
\d users
\d iiko_settings

-- Если нужные колонки отсутствуют, выполните миграцию:
-- (Обычно это делается автоматически при старте backend)
```

---

## Быстрая диагностика - Checklist

### Backend API
- [ ] Backend запущен (`docker ps` или `systemctl status`)
- [ ] Health endpoint отвечает (`curl http://localhost:8000/api/v1/health`)
- [ ] Логи без критических ошибок
- [ ] PostgreSQL доступен и работает
- [ ] CORS настроен правильно

### Frontend
- [ ] `.env` файл существует и содержит `BACKEND_API_URL`
- [ ] PHP-FPM запущен
- [ ] Nginx запущен и проксирует запросы
- [ ] Можно войти в админ-панель
- [ ] CSRF токены работают

### iiko API
- [ ] Новый API ключ получен из раздела "API"
- [ ] Ключ активен в iiko Cloud
- [ ] Ключ введён правильно (без пробелов)
- [ ] URL правильный: `https://api-ru.iiko.services/api/1`
- [ ] Organization ID указан (если требуется)

---

## Нужна помощь?

Если проблема не решена:

1. Соберите диагностическую информацию:
```bash
# Backend логи
docker-compose logs backend > backend-logs.txt
# или
sudo journalctl -u iiko-backend -n 200 > backend-logs.txt

# Frontend логи  
tail -n 100 /var/log/nginx/iiko-base-error.log > frontend-logs.txt

# API логи из базы данных
psql -h localhost -U iiko_user -d iiko_db -c "SELECT * FROM api_logs ORDER BY created_at DESC LIMIT 10;" > api-logs.txt
```

2. Создайте issue на GitHub с:
   - Описанием проблемы
   - Шагами для воспроизведения
   - Приложенными логами
   - Версией системы и окружением (Docker/systemd/etc)

---

**Последнее обновление:** 2026-02-09
