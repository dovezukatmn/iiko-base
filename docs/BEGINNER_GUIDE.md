# Руководство для новичков

## Добро пожаловать!

Это руководство предназначено для **новичков в веб-разработке**, которые хотят развернуть полноценное приложение на сервере. Не волнуйтесь, если многое непонятно - мы пройдем все шаг за шагом!

## Что такое iiko-base?

**iiko-base** - это готовая система для управления рестораном или кафе. Она состоит из:

1. **API (Application Programming Interface)** - программа на Python, которая обрабатывает запросы
2. **Админ-панель** - веб-сайт на PHP (Laravel), где вы управляете меню и настройками
3. **База данных** - PostgreSQL, где хранятся все данные
4. **Веб-сервер** - Nginx, который показывает ваш сайт пользователям

## Что вам понадобится?

### 1. VPS сервер

**VPS (Virtual Private Server)** - это как компьютер в интернете, который работает 24/7.

**Где получить:**
- Jino.ru - хорошо подходит для новичков
- DigitalOcean
- Hetzner
- Timeweb

**Минимальные требования:**
- 1 GB RAM (лучше 2 GB)
- 10 GB дискового пространства
- Ubuntu 20.04 или 22.04

### 2. Доменное имя (опционально)

**Домен** - это адрес вашего сайта (например, myrestaurant.com)

**Где купить:**
- Reg.ru
- Namecheap
- GoDaddy
- Включено в некоторые VPS тарифы

**Альтернатива:** Можно использовать IP адрес сервера (но домен лучше!)

### 3. SSH клиент

**SSH (Secure Shell)** - способ подключиться к серверу и управлять им.

**Windows:**
- PuTTY (скачать: https://www.putty.org/)
- Windows Terminal (встроено в Windows 10/11)

**Mac/Linux:**
- Terminal (встроено в систему)

### 4. Базовые знания

Вам НЕ нужно быть экспертом! Достаточно:
- Уметь копировать и вставлять команды
- Знать, что такое текстовый редактор
- Не бояться терминала (черного окна с текстом)

## Глоссарий терминов

Перед началом давайте разберемся с терминами:

- **Терминал/Консоль** - программа, где вы вводите команды текстом
- **SSH** - способ подключиться к удаленному серверу
- **Сервер** - компьютер, который работает 24/7 и обслуживает сайт
- **IP адрес** - уникальный номер сервера (например, 192.168.1.1)
- **Порт** - номер "двери" на сервере. Основной порт 80 для nginx, дополнительный порт 8000 для API backend
- **База данных** - программа для хранения данных (как Excel, но мощнее)
- **API** - программа, которая отвечает на запросы (как call-центр)
- **Backend** - невидимая часть сайта (логика, база данных)
- **Frontend** - видимая часть сайта (что видят пользователи)

## Пошаговая инструкция для полных новичков

### Шаг 1: Получите VPS сервер

1. Зарегистрируйтесь на Jino.ru (или другом хостинге)
2. Выберите тариф VPS (минимум 1GB RAM)
3. При заказе выберите:
   - **ОС:** Ubuntu 22.04
   - **SSH ключи:** Создайте или загрузите (хостинг поможет)
4. После оплаты вы получите:
   - IP адрес (например, 185.123.45.67)
   - Логин (обычно root)
   - Пароль или SSH ключ

### Шаг 2: Подключитесь к серверу

#### На Windows (PuTTY):
1. Скачайте и установите PuTTY
2. Запустите PuTTY
3. В поле "Host Name" введите IP адрес сервера
4. Нажмите "Open"
5. Если появится предупреждение о безопасности - нажмите "Yes"
6. Введите логин (обычно: root)
7. Введите пароль (при вводе символы не показываются - это нормально!)

#### На Mac/Linux (Terminal):
1. Откройте Terminal
2. Введите команду:
   ```bash
   ssh root@185.123.45.67
   ```
   (замените на ваш IP адрес)
3. Введите пароль

**Поздравляем! Вы подключились к серверу!**

### Шаг 3: Установка iiko-base

Теперь в терминале вы видите что-то вроде:
```
root@server:~#
```

Это означает, что вы готовы вводить команды.

#### 3.1. Скачайте проект

Скопируйте и вставьте эту команду (нажмите Enter):
```bash
cd /var/www
```

Затем:
```bash
git clone https://github.com/dovezukatmn/iiko-base.git
```

Перейдите в папку проекта:
```bash
cd iiko-base
```

#### 3.2. Запустите установку

Сделайте скрипты исполняемыми:
```bash
chmod +x scripts/*.sh
```

Запустите автоматическую установку:
```bash
./scripts/install.sh
```

**Что происходит:**
- Устанавливаются программы (Python, PHP, PostgreSQL, Nginx)
- Создается пользователь для приложения
- Настраивается база данных

**Время:** примерно 5-10 минут

**Подождите, пока не увидите:**
```
[INFO] Установка базовых компонентов завершена!
```

#### 3.3. Настройте окружение

Запустите:
```bash
./scripts/setup.sh
```

Вас спросят:
```
Хотите инициализировать базу данных? (y/n)
```

Введите: `y` (и нажмите Enter)

Затем:
```
Введите имя пользователя PostgreSQL [iiko_user]:
```

Просто нажмите Enter (использовать значение по умолчанию)

```
Введите пароль PostgreSQL:
```

Введите: `change_this_password` (или свой пароль, запомните его!)

```
Введите имя базы данных [iiko_db]:
```

Просто нажмите Enter

#### 3.4. Измените настройки

**Настройки Backend:**
```bash
nano backend/.env
```

**Что такое nano?** Это текстовый редактор в терминале.

Найдите строку:
```
DATABASE_URL=postgresql://iiko_user:your_password@localhost:5432/iiko_db
```

Измените `your_password` на пароль 12101991Qq!

Также измените:
```
SECRET_KEY=your-secret-key-change-this
```
на что-то уникальное (например, `my-super-secret-key-12345`)

**Как сохранить в nano:**
1. Нажмите `Ctrl + O` (буква О, не ноль)
2. Нажмите Enter
3. Нажмите `Ctrl + X`

**Настройки Frontend:**
```bash
nano frontend/.env
```

Найдите:
```
DB_PASSWORD=your_password
```

Измените на пароль 12101991Qq!

Сохраните (Ctrl+O, Enter, Ctrl+X)

#### 3.5. Настройте Nginx

```bash
nano nginx/iiko-base.conf
```

Найдите все места, где написано:
```
yourdomain.com
```

Замените на ваш домен (vezuroll.ru или b1d8d8270d0f.vps.myjino.ru)

Сохраните файл.

#### 3.6. Запустите деплой

```bash
./scripts/deploy.sh
```

Подождите, пока установятся все зависимости.

**Время:** примерно 5-10 минут

### Шаг 4: Проверьте, что все работает

Откройте браузер и перейдите:

**Если у вас домен:**
- http://vezuroll.ru
- http://b1d8d8270d0f.vps.myjino.ru

**Если только IP:**
- http://185.123.45.67 (ваш IP)

**API Backend:**
- http://api.vezuroll.ru/docs (или http://IP:8000/docs)

Вы должны увидеть документацию API!

### Шаг 5: Настройка домена (если есть)

#### 5.1. Добавьте DNS записи

В панели управления доменом (где вы его купили) добавьте:

**A записи:**
```
Имя: @
Тип: A
Значение: 185.123.45.67 (ваш IP)

Имя: www
Тип: A
Значение: 185.123.45.67 (ваш IP)

Имя: api
Тип: A
Значение: 185.123.45.67 (ваш IP)
```

**Подождите:** DNS изменения могут занять до 24 часов (обычно 1-2 часа)

#### 5.2. Получите SSL сертификат (HTTPS)

SSL сделает ваш сайт безопасным (с замочком в браузере).

На сервере выполните:
```bash
apt-get install certbot python3-certbot-nginx -y
```

Затем:
```bash
certbot --nginx -d vezuroll.ru -d www.vezuroll.ru -d api.vezuroll.ru
```

Следуйте инструкциям:
1. Введите email
2. Согласитесь с условиями (введите Y)
3. Выберите, хотите ли делиться email (на ваше усмотрение)

**Готово!** Теперь у вас HTTPS!

## Частые вопросы

### Что делать, если что-то пошло не так?

1. **Проверьте логи:**
   ```bash
   journalctl -u iiko-backend -f
   ```

2. **Перезапустите сервисы:**
   ```bash
   systemctl restart iiko-backend
   systemctl restart nginx
   ```

3. **Проверьте, запущены ли сервисы:**
   ```bash
   systemctl status iiko-backend
   systemctl status nginx
   systemctl status postgresql
   ```

### Как обновить проект?

```bash
cd /var/www/iiko-base
git pull origin main
./scripts/deploy.sh
```

### Как посмотреть логи ошибок?

**Python Backend:**
```bash
journalctl -u iiko-backend -n 100
```

**Nginx:**
```bash
tail -f /var/log/nginx/error.log
```

**Laravel:**
```bash
tail -f /var/www/iiko-base/frontend/storage/logs/laravel.log
```

### Как сделать backup базы данных?

```bash
pg_dump -U iiko_user iiko_db > backup.sql
```

Чтобы скачать на компьютер (на вашем компьютере):
```bash
scp root@your-server-ip:/root/backup.sql ~/Desktop/
```

### Как изменить пароль базы данных?

1. Подключитесь к PostgreSQL:
   ```bash
   sudo -u postgres psql
   ```

2. Измените пароль:
   ```sql
   ALTER USER iiko_user WITH PASSWORD 'new_password';
   \q
   ```

3. Обновите .env файлы с новым паролем

## Следующие шаги

После успешной установки:

1. **Изучите API:**
   - Откройте http://your-domain.com:8000/docs
   - Там интерактивная документация

2. **Настройте автоматический backup:**
   ```bash
   crontab -e
   ```
   Добавьте:
   ```
   0 2 * * * pg_dump -U iiko_user iiko_db | gzip > /backups/db_$(date +\%Y\%m\%d).sql.gz
   ```

3. **Настройте мониторинг:**
   - Установите Uptimerobot.com (бесплатно)
   - Он будет проверять, работает ли ваш сайт

4. **Начните разработку:**
   - Добавьте свои функции в Python backend
   - Создайте красивую админ-панель в Laravel

## Полезные ресурсы

**Документация:**
- [FastAPI](https://fastapi.tiangolo.com/) - Python backend
- [Laravel](https://laravel.com/docs) - PHP frontend
- [PostgreSQL](https://www.postgresql.org/docs/) - База данных

**Обучение:**
- [Codecademy](https://www.codecademy.com/) - интерактивное обучение
- [freeCodeCamp](https://www.freecodecamp.org/) - бесплатные курсы
- YouTube - множество туториалов

**Сообщество:**
- Stack Overflow - вопросы и ответы
- Reddit: r/learnprogramming
- Telegram группы по разработке

## Заключение

**Поздравляем!** Вы только что развернули полноценное веб-приложение!

Не бойтесь экспериментировать - вы всегда можете:
- Восстановить из backup
- Пересоздать VPS сервер
- Спросить помощи в сообществе

**Удачи в вашем проекте!** 🚀
