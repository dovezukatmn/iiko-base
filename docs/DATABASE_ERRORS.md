# Руководство по устранению ошибок базы данных PostgreSQL

Это подробное руководство поможет решить проблемы с подключением к PostgreSQL при запуске скрипта `setup.sh`.

## 📋 Содержание

1. [Ошибка: "Не удалось подключиться к PostgreSQL"](#ошибка-не-удалось-подключиться-к-postgresql)
2. [Причины возникновения ошибок](#причины-возникновения-ошибок)
3. [Пошаговое решение](#пошаговое-решение)
4. [Проверка и диагностика](#проверка-и-диагностика)
5. [Автоматическое создание пользователя](#автоматическое-создание-пользователя)
6. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Ошибка: "Не удалось подключиться к PostgreSQL"

### Симптомы

При запуске `./scripts/setup.sh` вы видите:

```
[INFO] Инициализация базы данных...
Хотите инициализировать базу данных? (y/n) y
Введите имя пользователя PostgreSQL [iiko_user]: 
Введите пароль PostgreSQL: 
Введите имя базы данных [iiko_db]: 
Введите хост PostgreSQL [localhost]: 
Введите порт PostgreSQL [5432]: 
[INFO] Проверка подключения к базе данных...
[ERROR] Не удалось подключиться к PostgreSQL
```

---

## Причины возникновения ошибок

### 1. **PostgreSQL не запущен**

PostgreSQL может быть установлен, но не запущен как служба.

**Как проверить:**
```bash
sudo systemctl status postgresql
```

**Решение:**
```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql  # автозапуск при загрузке системы
```

---

### 2. **Пользователь PostgreSQL не существует**

Скрипт `install.sh` должен был создать пользователя `iiko_user`, но это могло не произойти.

**Как проверить:**
```bash
sudo -u postgres psql -c "\du"
```

**Решение:**
```bash
sudo -u postgres psql
```

В интерактивной консоли PostgreSQL выполните:
```sql
CREATE USER iiko_user WITH PASSWORD 'ваш_безопасный_пароль';
ALTER USER iiko_user CREATEDB;
GRANT ALL PRIVILEGES ON DATABASE postgres TO iiko_user;
\q
```

---

### 3. **Неправильная настройка аутентификации (pg_hba.conf)**

PostgreSQL может требовать peer-аутентификацию вместо парольной для локальных подключений.

**Проблема:** По умолчанию PostgreSQL на Ubuntu использует `peer` аутентификацию для локальных подключений Unix-сокета, но нам нужна парольная аутентификация через TCP/IP.

**Как проверить:**
```bash
# Найдите файл pg_hba.conf
sudo find /etc/postgresql -name pg_hba.conf

# Посмотрите его содержимое
sudo cat /etc/postgresql/*/main/pg_hba.conf | grep -v "^#" | grep -v "^$"
```

**Решение:**

1. Откройте файл pg_hba.conf:
```bash
# Для PostgreSQL 14
sudo nano /etc/postgresql/14/main/pg_hba.conf

# Для PostgreSQL 12
sudo nano /etc/postgresql/12/main/pg_hba.conf
```

2. Добавьте или убедитесь, что присутствуют строки (перед строкой `local all all peer`):
```
# IPv4 local connections с парольной аутентификацией:
host    all             all             127.0.0.1/32            md5
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 md5
```

3. Сохраните файл (Ctrl+O, Enter, Ctrl+X) и перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

---

### 4. **Неправильный пароль**

Пароль, введенный при запуске `setup.sh`, не совпадает с паролем пользователя PostgreSQL.

**Решение:**

1. Сбросьте пароль пользователя:
```bash
sudo -u postgres psql
```

2. В консоли PostgreSQL:
```sql
ALTER USER iiko_user WITH PASSWORD 'новый_пароль';
\q
```

3. Запустите `setup.sh` снова и введите новый пароль.

---

### 5. **PostgreSQL не слушает на нужном порту**

PostgreSQL может быть настроен слушать другой порт или только Unix-сокет.

**Как проверить:**
```bash
sudo netstat -plnt | grep postgres
# или
sudo ss -plnt | grep postgres
```

Вы должны увидеть что-то вроде:
```
tcp  0  0  127.0.0.1:5432  0.0.0.0:*  LISTEN  1234/postgres
```

**Решение:**

Если порт 5432 не прослушивается, отредактируйте `postgresql.conf`:
```bash
sudo nano /etc/postgresql/*/main/postgresql.conf
```

Найдите и раскомментируйте/измените:
```
listen_addresses = 'localhost'
port = 5432
```

Перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

---

## Пошаговое решение

### Шаг 1: Проверка PostgreSQL

```bash
# Проверьте, установлен ли PostgreSQL
which psql

# Проверьте статус службы
sudo systemctl status postgresql

# Если не запущен, запустите
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Шаг 2: Создание пользователя (если нужно)

```bash
# Подключитесь как суперпользователь postgres
sudo -u postgres psql

# В консоли PostgreSQL выполните:
CREATE USER iiko_user WITH PASSWORD 'измените_этот_пароль';
ALTER USER iiko_user CREATEDB;
GRANT ALL PRIVILEGES ON DATABASE postgres TO iiko_user;

# Выйдите
\q
```

### Шаг 3: Настройка pg_hba.conf

```bash
# Откройте файл конфигурации
sudo nano /etc/postgresql/14/main/pg_hba.conf

# Добавьте перед строкой "local all all peer":
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5

# Сохраните и перезапустите
sudo systemctl restart postgresql
```

### Шаг 4: Тестирование подключения

```bash
# Проверьте подключение с флагом -h localhost (это важно!)
psql -h localhost -U iiko_user -d postgres

# Введите пароль, который вы установили
# Если подключение успешно, вы увидите приглашение postgres=#
```

### Шаг 5: Создание базы данных

```bash
# В консоли PostgreSQL:
CREATE DATABASE iiko_db OWNER iiko_user;

# Проверьте, что БД создана
\l

# Выйдите
\q
```

### Шаг 6: Повторный запуск setup.sh

```bash
cd /var/www/iiko-base  # или ваша директория проекта
./scripts/setup.sh
```

При запросе учетных данных введите:
- **Имя пользователя:** `iiko_user`
- **Пароль:** пароль, который вы установили на шаге 2
- **Имя БД:** `iiko_db`
- **Хост:** `localhost`
- **Порт:** `5432`

---

## Проверка и диагностика

### Полная диагностика

Запустите эти команды для полной диагностики:

```bash
# 1. Статус PostgreSQL
echo "=== PostgreSQL Status ==="
sudo systemctl status postgresql

# 2. Список пользователей
echo "=== PostgreSQL Users ==="
sudo -u postgres psql -c "\du"

# 3. Список баз данных
echo "=== Databases ==="
sudo -u postgres psql -c "\l"

# 4. Прослушиваемые порты
echo "=== Listening Ports ==="
sudo netstat -plnt | grep postgres

# 5. Версия PostgreSQL
echo "=== PostgreSQL Version ==="
psql --version

# 6. Проверка pg_hba.conf
echo "=== pg_hba.conf (active lines) ==="
sudo cat /etc/postgresql/*/main/pg_hba.conf | grep -v "^#" | grep -v "^$"
```

### Проверка подключения с деталями ошибки

```bash
# Попытка подключения с подробным выводом
PGPASSWORD='ваш_пароль' psql -h localhost -U iiko_user -d postgres -c '\conninfo'
```

---

## Автоматическое создание пользователя

Если вы запускаете скрипт установки впервые, выполните:

```bash
cd /var/www/iiko-base
sudo ./scripts/install.sh
```

Этот скрипт автоматически:
- Установит PostgreSQL
- Создаст пользователя `iiko_user` с паролем `change_this_password`
- Создаст базу данных `iiko_db`
- Настроит права доступа

**⚠️ ВАЖНО:** После установки обязательно измените пароль:

```bash
sudo -u postgres psql
ALTER USER iiko_user WITH PASSWORD 'ваш_безопасный_пароль';
\q
```

И обновите файлы `.env`:
- `backend/.env` - обновите `DATABASE_URL`
- `frontend/.env` - обновите `DB_PASSWORD`

---

## Часто задаваемые вопросы

### Q: Почему нужен флаг `-h localhost`?

**A:** Без флага `-h` psql пытается подключиться через Unix-сокет с peer-аутентификацией, которая не использует пароли. Флаг `-h localhost` заставляет использовать TCP/IP соединение с парольной аутентификацией (md5/scram-sha-256).

### Q: Что такое pg_hba.conf?

**A:** Это файл конфигурации PostgreSQL, который контролирует, какие пользователи могут подключаться к каким базам данных и какой метод аутентификации использовать.

### Q: В чем разница между md5 и scram-sha-256?

**A:** Оба метода аутентификации используют пароли, но:
- **md5** - старый, менее безопасный (но работает везде)
- **scram-sha-256** - современный, более безопасный (PostgreSQL 10+)

Рекомендуется использовать `scram-sha-256`, но `md5` подходит для большинства случаев.

### Q: Можно ли использовать другого пользователя вместо iiko_user?

**A:** Да, вы можете использовать любое имя пользователя. Просто создайте его как показано выше и введите при запуске `setup.sh`.

### Q: Что делать, если забыл пароль postgres?

**A:** Измените настройки аутентификации временно:

1. Откройте pg_hba.conf:
```bash
sudo nano /etc/postgresql/*/main/pg_hba.conf
```

2. Временно измените `md5` на `trust` для локальных подключений:
```
local   all             postgres                                trust
```

3. Перезапустите PostgreSQL:
```bash
sudo systemctl restart postgresql
```

4. Подключитесь без пароля и измените его:
```bash
sudo -u postgres psql
ALTER USER postgres WITH PASSWORD 'новый_пароль';
\q
```

5. Верните `md5` обратно в pg_hba.conf и перезапустите PostgreSQL.

---

## Загрузка исправлений в репозиторий

После внесения изменений в код:

```bash
# 1. Проверьте статус
git status

# 2. Добавьте изменения
git add scripts/setup.sh docs/DATABASE_ERRORS.md

# 3. Создайте коммит
git commit -m "Fix: Улучшена обработка ошибок PostgreSQL в setup.sh

- Добавлена проверка статуса PostgreSQL перед подключением
- Улучшены сообщения об ошибках с пошаговой диагностикой
- Добавлена проверка наличия psql клиента
- Создан файл DATABASE_ERRORS.md с подробными инструкциями
- Исправлена проблема с peer-аутентификацией"

# 4. Отправьте в репозиторий
git push origin main
```

---

## 📞 Дополнительная помощь

Если проблема не решена:

1. **Проверьте логи PostgreSQL:**
```bash
sudo tail -n 50 /var/log/postgresql/postgresql-*-main.log
```

2. **Создайте issue на GitHub** с результатами команд диагностики.

3. **Обратитесь в поддержку** с полным выводом:
```bash
./scripts/setup.sh 2>&1 | tee setup-error.log
```

---

**Создано с ❤️ для сообщества iiko-base**
